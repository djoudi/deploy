# Wordpress Multisite Mapping for Nginx.
map $http_host $blogid {
	default		0;
#	include		/home/masedi/Webs/masedi.net/wp-content/plugins/nginx-helper/map.conf;
}

server {
	listen   80; ## listen for ipv4; this line is default and implied
	#listen   [::]:80 default ipv6only=on; ## listen for ipv6

	# Make site accessible from world wide
	server_name masedi.net www.masedi.net direct.masedi.net;

	root /home/masedi/Webs/masedi.net;
	index index.php index.html index.htm;

	# Log
	access_log   /var/log/nginx/masedi.net.access.log;
	error_log    /var/log/nginx/masedi.net.error.log;

	# Global directives configuration.
	#include /etc/nginx/conf.vhost/block.conf;
	include /etc/nginx/conf.vhost/staticfiles.conf;
	include /etc/nginx/conf.vhost/restrictions.conf;
	
	# Custom Nginx directives
	#include /home/masedi/Webs/masedi.net/.ngxaccess;

	# Vhost directives configuration (WordPress single, multisite, or other default site config. Use only one config).
	#include /etc/nginx/conf.vhost/site_default.conf;
	#include /etc/nginx/conf.vhost/site_wordpress.conf;
	#include /etc/nginx/conf.vhost/site_wordpress_cached.conf;
	#include /etc/nginx/conf.vhost/site_wordpress-ms.conf;
	include /etc/nginx/conf.vhost/site_wordpress-ms_cached.conf;

	# Directive to avoid php readfile() for WordPress Multisite.
	location ^~ /blogs.dir {
		internal;
		alias /home/masedi/Webs/masedi.net/wp-content/blogs.dir;
		access_log off;
		log_not_found off;
		expires max;
	}

	# Pass all .php files onto a php-fpm/php-fcgi server.
	location ~ \.php$ {
		try_files $uri =404;
		
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
		#NOTE: You should have "cgi.fix_pathinfo = 0;" in php.ini
		
		fastcgi_index index.php;
		fastcgi_pass unix:/var/run/php5-fpm.masedi.sock;
		
		# Include FastCGI Params.
		include /etc/nginx/fastcgi_params;
		
		# Overwrite FastCGI Params here.
		#fastcgi_param SCRIPT_FILENAME	$document_root$fastcgi_script_name;
		fastcgi_param SCRIPT_NAME		$fastcgi_script_name;
		
		# Include FastCGI Configs.
		include /etc/nginx/conf.vhost/fastcgi.conf;

		# Uncomment to Enable PHP FastCGI cache.
		include /etc/nginx/conf.vhost/fastcgi_cache.conf;
	}
}

# PhpMyAdmin
server {
	listen 8082;

	server_name db.masedi.net;
	
	# Global directives configuration.
	include /etc/nginx/conf.vhost/staticfiles.conf;
	include /etc/nginx/conf.vhost/restrictions.conf;

	# PhpMyAdmin directives configuration.
	location /phpmyadmin {
		#root /usr/share/nginx/Webs/;
		root /home/masedi/Webs/;
		index index.php index.html index.htm;

		location ~ ^/phpmyadmin/(.+\.php)$ {
			#root /usr/share/nginx/Webs/;
			root /home/masedi/Webs/;

			try_files $uri =404;
			fastcgi_split_path_info ^(.+\.php)(/.+)$;
			#NOTE: You should have "cgi.fix_pathinfo = 0;" in php.ini
			
			fastcgi_index index.php;
			fastcgi_pass unix:/var/run/php5-fpm.masedi.sock;
			
			# Include FastCGI Params.
			include /etc/nginx/fastcgi_params;
			
			# Overwrite FastCGI Params here.
			#fastcgi_param	SCRIPT_FILENAME	$document_root$fastcgi_script_name;
			fastcgi_param	SCRIPT_NAME		$fastcgi_script_name;
			
			# Include FastCGI Configs.
			include /etc/nginx/conf.vhost/fastcgi.conf;

			# Uncomment to Enable PHP FastCGI cache.
			include /etc/nginx/conf.vhost/fastcgi_cache.conf;
		}

		location ~* ^/phpmyadmin/(.+\.(jpg|jpeg|gif|css|png|js|ico|html|xml|txt)) {
			#root /usr/share/nginx/Webs/;
			root /home/masedi/Webs/;
		}
	}

	location /phpMyAdmin {
		rewrite ^/* /phpmyadmin last;
	}
}

## Lab - lab.masedi.net
server {
	listen   80;
	server_name labs.masedi.net;

	root /home/masedi/Webs/labs.masedi.net;
	index index.php index.html index.htm;

	# Log
	access_log   /var/log/nginx/masedi.net.labs.access.log;
	error_log    /var/log/nginx/masedi.net.labs.error.log;

	# Global directives configuration.
	include /etc/nginx/conf.vhost/block.conf;
	include /etc/nginx/conf.vhost/staticfiles.conf;
	include /etc/nginx/conf.vhost/restrictions.conf;

	# Vhost directives configuration (WordPress single, multisite, or other default site config. Use only one config).
	include /etc/nginx/conf.vhost/site_default.conf;

	# Pass all .php files onto a php-fpm/php-fcgi server.
	location ~ \.php$ {
		try_files $uri =404;
		
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
		#NOTE: You should have "cgi.fix_pathinfo = 0;" in php.ini
		
		fastcgi_index index.php;
		fastcgi_pass unix:/var/run/php5-fpm.masedi.sock;
		
		# Include FastCGI Params.
		include /etc/nginx/fastcgi_params;
		
		# Overwrite FastCGI Params here.
		fastcgi_param SCRIPT_FILENAME	$document_root$fastcgi_script_name;
		fastcgi_param SCRIPT_NAME	$fastcgi_script_name;
		
		# Include FastCGI Configs.
		include /etc/nginx/conf.vhost/fastcgi.conf;

		# Uncomment to Enable PHP FastCGI cache.
		include /etc/nginx/conf.vhost/fastcgi_cache.conf;
	}
}

## Node.JS Test Env
server {
	listen		80;
	server_name	node.masedi.net;

	root /home/masedi/nodejs/;
	#index index.php index.htm index.html;

	# Logs
	access_log /var/log/nginx/masedi.net.access.log;

	# directives
	location / {
		include	/etc/nginx/fastcgi_params;
		proxy_pass	http://app_nodejs;
		proxy_redirect	off;
	}
}
